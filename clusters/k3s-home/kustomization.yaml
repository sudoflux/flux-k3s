apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Include the core Flux components generated by bootstrap
  - ./flux-system
  # Include the file that DEFINES the next layer Kustomizations
  - layers.yaml